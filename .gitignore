# Session/handoff state (local only)
.remember/

# llm-wiki per-project config (local only): autonomy mode + a per-user bundle_path override.
# NOTE: .claude/llm-wiki.md (no .local) is the COMMITTED repo-shared config — do NOT ignore it.
.claude/llm-wiki.local.md

# llm-wiki bundle state dir (capture-pending marker, consultations counter — local only).
# Glob (not the literal llm-wiki/.llm-wiki/) so it still matches a relocated bundle (bundle_path).
**/.llm-wiki/

# Python
__pycache__/
*.py[cod]

# OS / editor cruft
.DS_Store
*.swp
